---
description: 结构化输出与模型错误处理规则，定义真实模型返回解析、校验和中文错误提示
globs:
  - "**/*"
alwaysApply: true
---

# 结构化输出与模型错误处理规则

## 目标
真实模型输出必须经过结构化解析、schema 校验和中文错误处理，不得直接作为系统对象使用。

## 核心原则

### 1. 原始输出不等于系统对象
LLM 返回的 rawText 必须经过：
- JSON 解析
- Schema 校验
- Validator 校验

才可以进入 WorkflowPlanningDraft。

### 2. 失败时不得覆盖当前 Draft
若出现以下任一情况：
- 模型调用失败
- JSON 无法解析
- schema 校验失败
- Validator 校验失败

则不得覆盖当前 Draft，只能写入消息和错误提示。

### 3. 错误提示必须中文
前台至少支持以下中文提示：
- 流程规划助手调用失败
- 模型返回格式异常
- 草案结构校验失败
- 草案未通过规则校验，已保留当前版本
- 请检查输入后重试

### 4. 可允许有限重试
首版可允许：
- JSON 解析失败时自动重试 1 次
- 超时后提示用户重试

## 禁止事项
禁止：
- 将原始英文错误直接抛到前台
- 解析失败后仍然更新当前 Draft
- schema 校验失败后继续进入后续发布流程
