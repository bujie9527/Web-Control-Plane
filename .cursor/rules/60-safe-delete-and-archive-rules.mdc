---
description: 安全删除与归档规则，定义删除前引用检查与交互规范
globs:
  - "**/*"
alwaysApply: true
---

# 安全删除与归档规则

## 目标
删除操作必须安全可控，防止误删有引用的对象。

## 核心原则

### 1. 删除前必须执行引用检查
- 检查该对象是否被其他对象引用
- 引用存在则禁止删除
- 返回引用数量与引用类型

### 2. 引用检查服务
统一通过 referenceCheckService 提供：
- checkWorkflowTemplateReferences()
- checkAgentTemplateReferences()
- checkModelConfigReferences()
- checkProjectReferences()

### 3. 返回结构
```ts
{
  inUse: boolean
  referenceCount: number
  referenceTypes: string[]
}
```

### 4. 删除交互
- 弹窗确认
- 中文提示（如：该模板仍被 3 个流程实例使用，无法删除）
- 引用存在时禁用删除或给出明确错误

## 禁止事项
禁止：
- 未做引用检查即执行删除
- 引用存在时仍允许删除
- 错误提示使用英文
