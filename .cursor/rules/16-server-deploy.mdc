---
alwaysApply: true
---

# 服务器部署规则

## 服务器信息

- **IP**: 43.160.204.235
- **用户**: ubuntu
- **认证**: SSH 密钥（awcc_deploy）
- **操作系统**: Ubuntu Linux
- **域名**: ai.667788.cool
- **部署目录**: /opt/awcc
- **部署方式**: Git 化部署（GitHub → git pull → build → PM2）

## 部署流程

标准更新流程：
1. 本地完成开发并提交到 GitHub
2. 服务器执行 `cd /opt/awcc && bash scripts/deploy-update.sh`
3. 脚本自动执行：git pull → npm ci → prisma generate → vite build → prisma migrate deploy → pm2 restart

## 持久化数据

以下数据不在 Git 仓库中，部署时不会被覆盖：
- `server/data/app.db` — SQLite 数据库
- `server/data/credentials.json` — LLM 凭证
- `server/data/platform-integrations.json` — 平台集成配置
- `.env` — 环境变量（OPENAI_API_KEY 等）

## 隔离要求

1. API 服务仅监听本机回环地址（127.0.0.1:3001），Nginx 反向代理
2. Nginx 变更前必须执行 `nginx -t`
3. 部署后必须验证 `https://ai.667788.cool/` 可访问

## 禁止事项

- 禁止直接修改服务器上的代码（应通过 Git 更新）
- 禁止删除 server/data/ 目录下的持久化文件
- 禁止在生产环境直接运行 prisma migrate dev
