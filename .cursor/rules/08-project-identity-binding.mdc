---
alwaysApply: true
---

# Project Identity Binding Rules

## 关系定位

Identity / Persona 是项目的重要绑定资源。

项目、任务、终端、内容结果都应逐步与 Identity 建立关系。

---

## 当前阶段要求

当前阶段必须支持以下能力：

1. 项目可以绑定多个 Identity
2. 项目可以设置默认 Identity
3. 任务创建时可以选择本次使用的 Identity
4. 任务结果中显示所使用的 Identity
5. 项目结果回写中保留 Identity 信息
6. 终端预留 Identity 绑定位，但当前阶段可只做结构占位

---

## 关系规则

推荐关系约束如下：

### 项目与 Identity

- 一个项目可以绑定多个 Identity
- 一个项目可以设置一个默认 Identity

### 任务与 Identity

- 一个任务只能选择一个主 Identity
- 任务执行时必须明确“以谁的身份执行”

### 终端与 Identity

- 一个终端同一时间建议只绑定一个主 Identity
- 一个 Identity 可以被多个终端复用

---

## 页面要求

### 项目详情页

必须新增 Identity / 身份配置相关区域或页签。

### 任务创建页

必须支持选择 Identity。

### 任务详情页

必须展示本任务使用的 Identity。

---

## 产品原则

Identity 的作用不仅是“生成内容风格”，还包括：

- 统一表达口径
- 区分账号身份
- 支撑矩阵账号管理
- 为后续行为规则与平台适配提供基础对象

因此不得把它降级为简单标签字段。
