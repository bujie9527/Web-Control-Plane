---
description: LLM 凭证与服务端执行规则，定义凭证治理与服务端 LLM 调用链
globs:
  - "**/*"
alwaysApply: true
---

# LLM Credential 与服务端执行规则

## 目标
将 LLM 密钥治理与服务端执行纳入控制平面，实现可运营的 AI 平台安全基座。

## 核心原则

### 1. LLM 密钥必须存储在 Credential 对象中
- 密钥不得散落在 Provider 或环境变量中
- 密钥必须以 encryptedSecret 形式存储
- 前台永远不返回完整密钥，只返回 secretMasked

### 2. 密钥只能服务端读取
- 前台不得持有真实 API Key
- 所有 LLM 调用必须由服务端 Executor 执行
- Provider 通过 credentialId 关联凭证

### 3. 调用链结构
```
AgentTemplate → AgentLLMBinding → LLMModelConfig → LLMProvider → LLMCredential
  ↓
Server LLM Executor → Provider Adapter → OpenAI / 其他
```

### 4. Provider 结构
- LLMProvider 使用 credentialId，不再使用 apiKeyRef
- Provider 通过 credentialId 获取密钥（仅服务端）

## 禁止事项
禁止：
- 前台直接调用 OpenAI 或持有 API Key
- 将完整密钥返回给前端
- 绕过 Credential 对象存储密钥
