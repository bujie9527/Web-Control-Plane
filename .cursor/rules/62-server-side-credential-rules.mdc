---
description: 服务端凭证规则，要求真实密钥仅由服务端持有和使用
globs:
  - "**/*"
alwaysApply: true
---

# 服务端凭证规则

## 目标
LLM 凭证必须通过 LLMCredential 管理，并由服务端持有、读取和使用。

## 核心原则

### 1. 前台不得回显真实密钥
前台只能展示：
- 是否已配置
- 掩码密钥
- 状态

### 2. Provider 必须绑定 Credential
LLMProvider 不再长期依赖 apiKeyRef 自由文本，应通过 credentialId 关联凭证对象。

### 3. 真实模型调用必须从 Credential 解析密钥
服务端执行器应读取 LLMCredential 中的真实密钥后调用 Provider。

## 禁止事项
禁止：
- 将真实密钥重新暴露到前端环境变量作为正式方案
- 前台接口返回完整密钥
- Provider 缺少 Credential 仍冒然执行真实调用
