---
alwaysApply: true
---

# Project Goal Modeling Rules

项目创建流程中的目标定义必须采用结构化建模方式。

Goal 不能仅使用自由文本描述，而必须包含以下结构：

1. GoalType（目标类型）
2. PrimaryMetric（主目标指标）
3. SecondaryMetrics（辅助指标）
4. KPI说明

---

## 对象关系

ProjectType
   └ allowedGoalTypes[]

GoalType
   └ allowedMetricOptions[]

GoalMetricOption

---

## UI要求

Step 2 必须包含：

- 目标类型（单选）
- 主目标指标（单选）
- 辅助指标（多选）
- KPI说明（文本）

---

## 设计原则

Goal 建模必须：

- 明确目标方向
- 明确可衡量指标
- 保证数据可统计
- 保证流程推荐可用

禁止将 Goal 设计为普通备注字段。
