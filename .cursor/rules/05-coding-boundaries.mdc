---
alwaysApply: true
---

# Coding Boundaries Rules

## 修改范围原则

修改代码前必须：

1️⃣ 确认影响模块  
2️⃣ 限制改动范围  

不要：

修改不相关文件。

---

## 组件复用原则

新增页面前必须检查：

是否已有组件。

优先复用：

- 表格
- 卡片
- 抽屉
- 表单

---

## 页面逻辑限制

页面组件不得包含：

- 复杂业务逻辑
- 第三方平台调用

必须通过：

service / provider。

---

## 复杂任务规则

复杂任务必须：

先输出计划。

计划包括：

- 影响文件
- 新增模块
- 修改模块
- 风险点

确认后再执行。

---

## 完成后自检

修改完成后必须检查：

- 是否符合架构
- 是否符合 UI
- 是否复用组件
- 是否破坏模块边界
