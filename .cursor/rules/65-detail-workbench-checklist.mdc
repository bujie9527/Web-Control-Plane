---
alwaysApply: true
---

# 详情工作台标准能力清单

## 适用范围

凡是以"资源详情"为主体、需要展示多维度信息的页面，包括但不限于：
- 项目详情工作台（ProjectDetailWorkbench）
- 租户详情页（TenantDetail）
- Agent 详情页（AgentLibraryDetail）
- 身份详情工作台（IdentityDetailWorkbench）
- 流程模板详情（WorkflowTemplateDetail）
- 终端详情页

开发或修改上述页面时，必须对照本清单逐项确认。

---

## 1. 顶部返回条（Back Bar）

必须包含：

- [ ] **返回按钮**（← 文字或图标），点击返回上一级列表页
- [ ] **面包屑路径**：如「项目中心 / 演示项目」，体现当前层级位置
- [ ] 返回条固定在页面最顶部，不随内容滚动消失

---

## 2. 资源摘要条（Header Summary Bar）

固定在返回条下方，必须包含：

- [ ] **资源名称**（主标题，大字体）
- [ ] **核心状态标签**：使用 StatusTag 组件，显示中文枚举状态
- [ ] **关键字段摘要**（3～5 个）：如类型、创建时间、所属租户、关联数量等，选与当前资源最相关的字段
- [ ] **主操作按钮区**（右侧对齐）：
  - 编辑按钮（打开编辑抽屉或跳转编辑页）
  - 状态切换按钮（如启用/停用，视资源类型决定）
  - 删除按钮（触发二次确认弹窗，含资源名称）

---

## 3. Tab 导航（页内二级导航）

- [ ] **水平 Tab 栏**，固定在摘要条下方
- [ ] Tab 切换使用本地 state 控制，**不触发页面跳转、不刷新页面**
- [ ] 当前激活 Tab 有高亮样式
- [ ] Tab 数量建议 3～8 个，不宜过多
- [ ] Tab 命名使用中文，简洁清晰

### 各资源类型标准 Tab 结构

**项目详情：**
概览 / 目标与KPI / 身份配置 / Agent团队 / 终端分配 / 流程任务 / 结果反馈 / 项目设置

**Agent 详情：**
基础信息 / LLM绑定 / Skills配置 / 执行记录

**身份（Identity）详情：**
基础信息 / 表达规则 / 行为规则 / 平台适配 / 绑定项目 / 绑定终端

**流程模板详情：**
模板信息 / 节点结构 / 绑定 Agent / 发布记录

**租户详情：**
基本信息 / 套餐与配额 / 成员管理 / 项目概览 / 操作记录

---

## 4. Tab 内容区规范

每个 Tab 内容必须：

- [ ] **用独立子组件文件实现**（如 `OverviewTab.tsx`、`SettingsTab.tsx`），不堆在主组件里
- [ ] **用 Card 组件分区展示**，每个语义分组一个 Card
- [ ] **有数据时**：正常展示内容
- [ ] **无数据时**：使用 EmptyState 组件，说明为空原因，提供引导操作（如「去配置」「去绑定」）
- [ ] **加载中**：显示 loading 状态

### 每个 Tab 文件的标准结构

```
tabs/
  OverviewTab.tsx       # 概览（必须有）
  SettingsTab.tsx       # 设置（必须有）
  {其他业务Tab}.tsx
```

---

## 5. 编辑能力

- [ ] 摘要条的编辑按钮，触发**编辑抽屉**（Drawer）或跳转编辑页
- [ ] 编辑抽屉预填当前资源数据
- [ ] 保存成功后，刷新页面数据（摘要条和 Tab 内容同步更新）
- [ ] 保存失败时，抽屉不关闭，显示错误原因

---

## 6. 删除能力

- [ ] 摘要条有删除按钮
- [ ] 触发 Dialog 二次确认，正文明确显示资源名称
- [ ] 删除成功后，跳转回列表页
- [ ] 删除前检查引用关系（如该资源被其他资源引用，需提示）

---

## 7. 概念约束（必须遵守）

- **Identity（身份）** 属于项目层配置，**不是 Agent 的属性**，不得在 Agent 详情中展示身份字段
- **Agent** 是垂直能力单元（LLM + Skills），不承载人格
- **Terminal（终端）** 绑定到项目的 Identity 下，不直接绑定到 Agent
- 详情页只调用 service 层，不直接访问 repository 或 mock 数据

---

## 开发完成后必须自检

```
□ 顶部返回条：返回按钮 + 面包屑，不随内容滚动
□ 摘要条：资源名称、状态标签、3~5 个关键字段、编辑/状态切换/删除按钮
□ Tab 导航：水平排列、state 控制切换、不刷页、当前 Tab 高亮
□ 每个 Tab 是独立子组件文件（tabs/ 目录）
□ Tab 内用 Card 分区，空状态用 EmptyState
□ 编辑：抽屉预填数据，保存后同步更新摘要条和 Tab 内容
□ 删除：含确认弹窗 + 资源名称，成功后跳回列表
□ 未混用 Identity / Agent / Terminal 概念
□ 页面只调用 service，未直连 mock 或 repository
```

凡是遗漏上述任何一项，必须补全后再提交。
