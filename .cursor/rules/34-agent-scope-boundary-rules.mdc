---
description: Agent 作用域边界规则，定义 Project / Workflow / Runtime 的 Agent 职责分层
globs:
  - "**/*"
alwaysApply: true
---

# Agent 作用域边界规则

## 目标
明确 Project、Workflow、Runtime 三层中 Agent 的职责边界，避免重复配置和语义冲突。

## 核心原则

### 1. Project 层只负责 Agent 范围与偏好
Project 可以定义：
- allowedAgentTemplateIds
- preferredAgentTemplateIds
- defaultPlannerAgentTemplateId
- defaultSupervisorAgentTemplateId（预留）

Project 不直接决定具体节点由哪个 Agent 执行。

### 2. Workflow 层负责 Agent 推荐与执行绑定
WorkflowTemplateNode / WorkflowPlanningDraftNode 应负责：
- recommendedAgentTemplateId
- allowedAgentRoleTypes
- allowedSkillIds
- executorStrategy
- reviewPolicy

### 3. Runtime 层负责实际执行选择
WorkflowInstanceNode / AgentBinding 负责：
- selectedAgentTemplateId
- selectedSkillIds
- runtimeExecutorStrategy
- fallbackAgentTemplateId（后续预留）

## 前台语义要求
项目层相关文案必须改为：
- 可用 Agent 范围
- 推荐 Agent 组合
- 默认流程规划助手

禁止使用误导性文案：
- 项目执行 Agent
- 项目绑定 Agent
- 项目节点 Agent

## 禁止事项
禁止：
- 在 Project 层配置节点级执行 Agent
- Project 与 Workflow 同时承担同一层级的 Agent 绑定
- 让项目编辑页与流程节点页产生双重执行绑定语义
