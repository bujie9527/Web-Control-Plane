---
description: 身份与人设模型：Identity 资源对象、Auth 登录身份、命名与分层约定
alwaysApply: true
---

# Identity / Persona Model Rules

## 模块定位

Identity / Persona 是本项目中的核心标准对象，不是普通文本字段，不是备注信息，也不是只存在于 Prompt 中的隐性设定。

Identity / Persona 用于定义：

- 账号是谁
- 以什么立场说话
- 用什么风格表达
- 有哪些内容边界
- 有哪些行为边界
- 在不同平台如何适配表达与行为

---

## 当前阶段目标

当前阶段只实现 Identity 1.0，不做复杂智能逻辑。

Identity 1.0 重点包括：

1. Identity Library（身份库）
2. Identity 标准对象 schema
3. Identity 列表页
4. Identity 详情页 / 工作台型详情骨架
5. 项目绑定 Identity
6. 任务创建时选择 Identity
7. 终端预留绑定 Identity 的结构位
8. 任务结果与项目结果中展示 Identity 信息

---

## 非目标

当前阶段不做：

- 复杂 Persona 自动生成
- 基于 Identity 的真实模型编排
- 基于 Identity 的自动行为控制引擎
- 基于 Identity 的复杂策略系统
- Prompt 自动编译器

---

## 对象原则

Identity 必须被建模为标准资源对象，至少应具备：

- id
- tenantId
- name
- type
- corePositioning
- toneStyle
- contentDirections
- behaviorRules
- platformAdaptations
- status
- createdAt
- updatedAt

---

## 结构原则

Identity 不得仅以以下形式存在：

- 页面备注
- Prompt 文本片段
- 账号附注字段
- 临时表单说明

Identity 必须拥有：

- 独立 schema
- 独立 service
- 独立 mock service
- 可绑定到项目
- 可绑定到任务
- 可预留绑定到终端

---

## 页面原则

Identity 模块页面必须体现：

- 它是可复用资源库
- 它是项目与账号行为的一致性约束来源
- 它不是简单“人设文案库”

---

## 命名原则

在本项目中，统一优先使用：

- Identity
- Persona
- Identity Profile
- Persona Profile

避免混乱命名，例如：

- character
- role setting
- style note
- profile note

如需在中文页面中展示，建议使用：

- 身份
- 人设
- 身份档案
- 人设档案

---

## 开发原则

- 先完成结构化资源管理，再考虑智能逻辑
- 先完成项目绑定与任务使用，再考虑终端自动约束
- 页面与 service 分层清晰
- 不把 Identity 逻辑直接写入项目页面组件

---

## Auth 身份（登录与后台类型）

与上述「Identity 资源对象」区分：此处指当前登录用户与后台类型，用于路由守卫与租户作用域。

- **User**：`id`、`name`、`account`、`role`（RoleCode）、可选 `tenant`（TenantContext）。
- **AuthState**：`user | null`、`consoleType`（platform | tenant）、可选 `scopes`（预留）。
- **ConsoleType**：由 `user.tenant` 决定——有 tenant 则为 `tenant`，否则为 `platform`。登录后不可在会话中切换身份侧。
- **RoleCode**：与 04 领域模型一致（platform_owner、tenant_admin、project_manager 等）；扩展仅在 `core/types/auth.ts`。
- 判断「当前是平台还是租户」统一用 `useAuth().consoleType`；租户名/ID 从 `useAuth().user?.tenant` 取。
- Mock 用户：`core/auth/mockUsers.ts`；持久化 key 为 `awcc_auth`。

禁止：平台后台用「当前租户」做数据筛选；租户后台用平台身份或跨租户 ID；在组件内硬编码角色字符串（应引用 RoleCode 或常量）。
