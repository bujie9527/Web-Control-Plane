---
description: 系统管理员与租户的 Agent 模板权限边界规则
globs:
  - "**/*"
alwaysApply: true
---

# 系统管理员与租户权限边界规则

## 目标
Agent Template Factory 是平台级系统能力，不是租户后台能力。

只有系统管理员（system admin / platform admin）可以管理 Agent 模板。
租户只能使用已发布模板，不能创建、编辑、复制、发布或归档模板。

## 权限划分

### 系统管理员可执行
- 访问 Agent Template Factory 列表页
- 查看模板详情
- 创建模板
- 编辑模板
- 复制模板
- 修改模板状态
- 管理模板版本
- 查看模板被引用情况

### 租户管理员 / 租户用户可执行
- 在项目中查看可用模板
- 在 Workflow 节点中选择模板
- 查看模板只读说明
- 在授权范围内配置节点绑定

### 租户管理员 / 租户用户禁止执行
- 创建模板
- 编辑模板
- 复制模板
- 修改模板状态
- 改写模板的 Skill / Executor / Terminal 边界
- 改写模板的 Guardrails
- 绕过平台审核策略和系统约束

## 页面与路由边界
系统管理员专属路由建议：
- /system/agent-factory
- /system/agent-factory/new
- /system/agent-factory/[id]
- /system/agent-factory/[id]/edit

这些页面只能在 system admin 权限范围内可见和可访问。

租户侧只能看到：
- AgentTemplateSelector
- 模板只读说明弹窗或只读面板
- Workflow 节点中的模板绑定区

## UI 要求
必须在 UI 结构上明确区分：
- 模板工厂管理页面
- 模板消费/绑定页面

不允许让租户页面看起来像在编辑系统模板。

## 工程要求
若项目已有权限体系，必须复用现有权限机制。
若当前尚未完成完整权限体系，必须先提供 mock guard 或占位权限守卫，确保结构上不混淆。

## 禁止事项
禁止：
- 把模板工厂入口暴露给租户
- 把平台模板设计成 tenant-owned
- 在租户工作流中隐式提供模板编辑能力
- 因为首版使用 mock 数据而省略权限边界

## 最终目标
系统必须明确体现：
- 模板属于平台
- 绑定属于租户项目
- 管理与使用是两套完全不同的权限与界面语义
