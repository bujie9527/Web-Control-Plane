---
description: Provider 测试连接与状态规则，定义模型提供商的测试、启停与交互反馈
globs:
  - "**/*"
alwaysApply: true
---

# Provider 测试连接与状态规则

## 目标
模型提供商不仅要可展示，还要支持基础测试连接与状态切换。

## 核心原则

### 1. 提供商测试连接按钮必须可执行
本阶段可先使用 mock 结果，但交互必须完整：
- 可点击
- 有提交中状态
- 可返回成功/失败
- 有中文提示

### 2. Provider 必须支持启用与停用
状态至少包括：
- `active`
- `disabled`

### 3. 前台必须有中文反馈
至少支持：
- 测试连接成功
- 测试连接失败
- 提供商未启用，无法测试
- 缺少接口地址
- 缺少密钥引用

## 禁止事项
禁止：
- 长期保留“测试连接”按钮为 disabled
- 切换状态后前台无反馈
- 状态按钮仅用不明显的文本链接且交互体验差（应至少有明确的反馈与可发现性）

