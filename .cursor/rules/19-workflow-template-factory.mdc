---
description: Workflow 模板工厂规则，定义平台级与租户级流程模板对象、约束和边界
globs:
  - "**/*"
alwaysApply: true
---

# Workflow 模板工厂规则

## 目标
Workflow Template Factory（流程模板工厂）是 AI Work Control Center 中用于定义、治理、发布、复制、复用流程模板的核心模块。

它不是：
- 通用 BPM 引擎
- 无边界自由流程编辑器
- 单纯的任务列表配置页
- 运行时执行器

它是：
- 面向 AI 执行项目的流程模板工厂
- 项目语义边界下的流程定义中心
- Workflow 节点、Agent、Skill、交付模式之间的结构化桥梁
- 平台流程资产与租户流程资产的治理基础

## 架构位置
系统主链路必须保持：

Project → Workflow Template / Workflow Instance → Node Binding → Agent Template → Skill → Executor → Terminal → Result

其中：
- Workflow Template 属于流程定义层
- Workflow Instance 属于流程运行层
- Workflow Template 必须服务于 Project 预设目标和交付模式
- Workflow Template 不能脱离 Project 语义独立存在

## 核心原则

### 1. 流程模板必须强绑定项目语义边界
每一个 Workflow Template 必须至少绑定：
- 一个 supportedProjectTypeId
- 一组受限的 supportedGoalTypeIds
- 一组受限的 supportedDeliverableModes
- 一组受限的 supportedChannels（如适用）

流程模板不能成为跨项目类型无约束复用的自由流程。

### 2. 流程可以细化目标，不能改写目标
项目目标在项目创建时已经建模完成，属于上层不可变业务边界。
Workflow Template 只能拆解执行步骤，不能改写项目主目标语义。

### 3. 流程模板必须支持结构化节点，而不是自由文本步骤
流程的每个节点都必须是结构化节点对象，具备明确的节点类型、顺序、约束、输入输出与执行绑定能力。

### 4. 流程模板必须支持平台模板与租户模板两层体系
- 平台管理员可以创建 system scope 的流程模板
- 租户可以复制平台模板并保存为 tenant scope 的私有模板
- 租户不能直接修改平台模板定义

### 5. 流程模板必须服务于固定交付模式
Workflow Template 必须声明其适配的交付模式范围，并与项目的交付模式约束保持一致。

## Workflow Template 必须包含的结构维度

### 1. 基础身份
- id
- name
- code
- description

### 2. 作用域与所有权
- scopeType: system | tenant
- tenantId（仅 tenant scope 使用）

### 3. 生命周期
- status: draft | active | deprecated | archived
- version
- isLatest
- isSystemPreset

### 4. 来源与复制关系
- sourceTemplateId
- sourceVersion
- clonedFromTemplateId（如适用）

### 5. 项目适配边界
- supportedProjectTypeId
- supportedGoalTypeIds
- supportedDeliverableModes
- supportedChannels
- supportedIdentityTypeIds（如适用）

### 6. 流程规划模式
- planningMode: manual | ai_assisted | hybrid

### 7. 推荐执行信息
- recommendedAgentTemplateIds
- recommendedSkillIds（可选）
- defaultReviewPolicy（可选）

### 8. 统计信息
- nodeCount
- createdAt
- updatedAt

## 设计边界
禁止：
- 创建不绑定 ProjectType 的流程模板
- 创建不绑定交付模式的流程模板
- 允许流程模板改变项目目标主语义
- 把流程模板设计成纯文本 SOP 仓库
- 把流程模板和运行时实例混为一体
- 让租户直接编辑平台模板

## 必须遵守的设计要求
必须：
- 把 Workflow Template 设计成结构化流程模板对象
- 支持 system / tenant 两层作用域
- 支持平台模板复制为租户模板
- 为后续节点构建器、SOP 规划器、运行态中心预留接口
- 保持流程定义层与运行层清晰分离
