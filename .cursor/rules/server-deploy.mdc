# 服务器部署规则

## 服务器信息

- **IP**: 43.165.195.151
- **用户**: root
- **认证**: SSH 密钥（已配置免密登录）
- **密码备用**: Ye972030
- **操作系统**: OpenCloudOS 9.4

-使用域名 ai.sweetygirl.ai
-不要影响服务器内其他运行的网站

## 线上部署与隔离要求（必须）

1. 仅允许新增 `ai.sweetygirl.ai` 的独立站点配置文件（例如 `ai.sweetygirl.ai.conf`）。
2. 禁止修改服务器内其他站点的 `server_name`、`root`、反向代理与证书配置。
3. API 服务仅监听本机回环地址（`127.0.0.1`），不得直接暴露公网端口。
4. Nginx 变更前必须执行 `nginx -t`，校验失败时禁止 reload/restart。
5. 发布必须提供可执行回滚步骤（至少包含：恢复上一个 dist、恢复上一版 Nginx 配置、重载 Nginx）。
6. 部署后必须验证：
   - `https://ai.sweetygirl.ai/` 可访问前端
   - `https://ai.sweetygirl.ai/api/credentials` 可访问后端 API
   - 其他已运行站点访问不受影响


