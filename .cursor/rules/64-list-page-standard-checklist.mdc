---
alwaysApply: true
---

# 列表页标准能力清单

## 适用范围

凡是以"资源列表"为主体的页面，包括但不限于：
- 项目列表、Agent 列表、Skills 列表、终端列表
- 租户列表、用户列表、模板列表、凭证列表
- 任何形如「XXX 管理」「XXX 中心」的列表型页面

开发或修改上述页面时，必须对照本清单逐项确认。

---

## 1. 顶部工具栏（ListPageToolbar）

必须包含：

- [ ] **新建按钮**：主操作入口，触发新建表单（抽屉或跳转页）
- [ ] **关键词搜索框**：支持按名称/标识模糊搜索
- [ ] **状态筛选**：下拉选择，筛选 active / disabled / draft / archived 等状态
- [ ] **类型/分类筛选**（如有多类型）：下拉选择
- [ ] **批量操作区**：选中行数 > 0 时显示，包含批量删除等操作（见第 4 节）

---

## 2. 表格列（Table Columns）

必须包含：

- [ ] **名称列**：可点击跳转详情，或打开详情抽屉
- [ ] **标识/编码列**（如有 code / id 展示需求）
- [ ] **状态列**：使用 StatusTag 组件，枚举中文标签
- [ ] **关键业务字段**：视资源类型决定（如租户数、节点数、绑定数等）
- [ ] **创建时间 / 更新时间**：至少保留其中一列
- [ ] **操作列**（固定在最右侧）：见第 3 节

---

## 3. 行级操作（Row Actions）

每行操作列必须包含：

- [ ] **查看** 或 **详情**：跳转详情页或打开详情抽屉（视页面层级决定）
- [ ] **编辑**：跳转编辑页或打开编辑抽屉，加载当前行数据
- [ ] **删除**：触发二次确认弹窗，弹窗中必须显示被删除对象的名称；确认后调用 service 执行删除，成功后刷新列表
- [ ] **状态切换**（如资源有启用/禁用概念）：如「启用」/「停用」，操作后刷新行状态

操作列要求：
- 操作项不超过 4 个时，直接显示文字按钮（不使用下拉菜单）
- 操作项超过 4 个时，次要操作收入「更多」下拉

---

## 4. 批量操作（Batch Actions）

- [ ] 表格第一列为 Checkbox，支持全选 / 单选
- [ ] 选中后，工具栏显示「已选 N 项」及批量操作按钮
- [ ] **批量删除**：触发二次确认，提示「将删除 N 个 XXX，不可恢复」；确认后逐一删除或批量调用 service
- [ ] 批量操作完成后，清空选中状态并刷新列表

---

## 5. 删除确认弹窗标准

- [ ] 标题：「删除确认」或「删除 {资源名称}」
- [ ] 正文：明确告知被删除对象名称，如「确定要删除「{name}」吗？此操作不可撤销。」
- [ ] 按钮：取消（左）/ 确认删除（右，危险色）
- [ ] 删除成功后：Toast 提示「已删除」；列表自动刷新
- [ ] 删除失败后：Toast 显示错误原因；弹窗关闭，列表不变

---

## 6. 空状态（EmptyState）

- [ ] 列表无数据时，显示 EmptyState 组件
- [ ] 空状态文案说明当前为空的原因，如「暂无项目，点击新建开始」
- [ ] 空状态内提供**新建入口**按钮（主操作快捷入口）
- [ ] 搜索/筛选后无结果时，显示「未找到匹配项，请调整搜索条件」，不显示新建按钮

---

## 7. 分页（Pagination）

- [ ] 列表底部有 Pagination 组件
- [ ] 显示总条数，如「共 42 条」
- [ ] 支持切换每页条数（10 / 20 / 50）
- [ ] 翻页后保留搜索/筛选条件

---

## 8. 加载与错误状态

- [ ] 数据加载中：表格区域显示 loading 状态（骨架屏或 spinner）
- [ ] 加载失败：显示错误提示，提供「重试」入口

---

## 开发完成后必须自检

完成列表页开发后，逐项检查：

```
□ 工具栏：新建、搜索、筛选、批量操作区
□ 表格列：名称可点击、状态标签、时间列、操作列
□ 行操作：查看、编辑、删除（含确认弹窗）
□ 批量：Checkbox 全选、批量删除
□ 空状态：有新建入口、搜索无结果区分
□ 分页：总数、翻页、每页条数
□ 加载与错误态
```

凡是遗漏上述任何一项，必须补全后再提交。
