---
alwaysApply: true
---

# 项目领域模型规则

## 模块定位

Project 在本系统中不是普通管理对象，而是业务执行容器。

Project 必须具备以下核心业务含义：

- 明确目标（Goal）
- 明确交付标的（Deliverable）
- 明确资源配置（Resources）
- 明确身份配置（Identities）
- 明确终端配置（Terminals）
- 明确 SOP（自然语言操作流程）
- 为 Workflow / Task 执行提供上下文

---

## 1. 核心子对象

必须引入以下标准子对象，作为显式结构存在，不能只作为自由文本散落在页面中：

### 1.1 ProjectGoal

用于定义项目目标，必须具备稳定性和约束性。目标确认后不应轻易漂移。

Goal 必须采用结构化建模，不能仅使用自由文本描述：

- **GoalType**（目标类型）— 单选
- **PrimaryMetric**（主目标指标）— 单选
- **SecondaryMetrics**（辅助指标）— 多选
- **KPI 说明** — 文本

对象关系：

```
ProjectType
  └ allowedGoalTypes[]

GoalType
  └ allowedMetricOptions[]

GoalMetricOption
```

Goal 建模必须：明确目标方向、明确可衡量指标、保证数据可统计、保证流程推荐可用。禁止将 Goal 设计为普通备注字段。

### 1.2 ProjectDeliverable

用于定义项目交付标的，是项目可验收的输出要求。

### 1.3 ProjectResourceConfig

用于定义项目可使用的资源，如：Identity、Terminal、Server、API、AgentTeam。

### 1.4 ProjectSOP

SOP 必须分为：

- sopRaw（人工自然语言输入）
- sopParsed（结构化解析结果，占位即可）

---

## 2. Goal / SOP / Workflow 边界（严格分离）

在本系统中，Goal、SOP、Workflow 不是同一个概念，必须严格区分。

| 对象 | 回答的问题 | 本质 |
|---|---|---|
| **Goal** | "这个项目为什么存在，要达成什么结果？" | 业务目标，不是执行步骤 |
| **Deliverable** | "这个项目具体交付什么？" | 可验收的输出要求 |
| **SOP** | "这件事总体怎么做？" | 项目级自然语言操作方案，不是流程模板也不是任务实例 |
| **WorkflowTemplate** | "系统预设有哪些标准流程可选？" | 结构化模板，不等于 Goal 或 SOP |
| **WorkflowInstance** | "当前项目这次具体怎么执行？" | 一次具体执行，来源于模板 + Goal + SOP + Identity |
| **Task** | 业务层的执行对象展示与操作单元 | 必须来源于 WorkflowInstance 或 WorkflowInstanceNode，不脱离流程独立存在 |

---

## 3. SOP → Workflow 转换规则

当前阶段允许：

- 根据 Goal / Deliverable / SOP 推荐流程模板
- 根据 SOP 生成建议流程草稿占位
- 人工确认后创建 WorkflowInstance

当前阶段不允许：

- 把 SOP 直接当作 WorkflowTemplate
- 把 SOP 直接自动变成不可追踪的 Task 列表
- 把 Goal / SOP / Workflow 混为同一对象

开发原则：

- 先建立"推荐 / 解析建议 / 人工确认"机制
- 先保证可控和可追踪
- 后续再增强自动拆解与自动执行

---

## 4. 项目创建多步骤流程

Project Creation Flow 是系统中"从业务想法进入系统项目"的入口层。它的职责不是简单创建一个 Project 记录，而是通过结构化步骤，把一个真实业务项目初始化为可执行的项目上下文。

### 4.1 推荐步骤顺序

1. 项目基础信息
2. 目标与交付模式
3. 流程与任务
4. Agent 范围与协作偏好
5. 身份 / 渠道 / 资源
6. 预算与审核
7. 确认

关键顺序原则：**流程是目标的执行实现**，目标确定后应立即确认流程。Agent 范围配置应位于流程选择之后，因为 Agent 范围是对流程执行的支持性约束。

### 4.2 创建原则

项目创建不是单一表单，不应只包含名称、描述、状态。项目创建必须体现业务约束与执行上下文的建立过程。

### 4.3 交互原则

当前阶段优先使用：多步骤向导、分步骤确认、摘要回顾、最终确认创建。不使用一页长表单堆叠所有信息。

### 4.4 数据原则

项目创建后，必须至少能生成或写入：

- Project
- ProjectGoal
- ProjectDeliverable
- ProjectResourceConfig
- ProjectSOP
- Identity 绑定关系
- Terminal 绑定关系（当前阶段允许轻占位）
- 推荐流程占位信息

### 4.5 页面一致性

项目创建页与项目编辑页应保持大体一致的结构顺序，避免用户在创建与编辑时形成不同认知模型。

---

## 5. 最小真实案例：Facebook 公共主页运营

所有项目创建流程、字段设计、页面结构和 mock 数据，都应优先围绕以下最小真实案例进行设计和验证：

### 案例信息

| 维度 | 内容 |
|---|---|
| **Goal** | 运营 Facebook 公共主页；提升活跃度与互动；获取潜在线索 |
| **Deliverable** | 每天发布内容；每周提升互动；收集潜在客户联系意向 |
| **Identity** | 明确一个适配该主页的身份档案 |
| **Terminal** | Facebook Page 终端；Web 自动化终端（当前可占位） |
| **SOP** | 用自然语言描述该主页内容运营方式 |

### 案例设计原则

- 优先让此案例成立，再在结构上保持未来可扩展
- 不要为了通用性而把流程抽象得过空
- 该案例是真实业务场景，目标明确，身份价值明显，终端与执行路径清晰，适合作为后续矩阵运营的起点

---

## 6. 页面原则

- 项目详情页必须成为核心子对象的主要工作台
- 不得把 Goal / Deliverable / Resources / SOP 降级为零散表单字段
- Identity 应属于项目上下文或流程实例上下文，而不是 Goal 本身

---

## 7. 当前阶段目标与非目标

### 当前阶段目标

- 完成对象模型与页面承载结构
- 建立 Goal / Deliverable / Resources / SOP 的对象模型
- 在项目详情页承载这些模块
- 为后续 Workflow & Task Execution 留好结构位

### 非目标

当前阶段不做：

- 复杂自动执行 / 复杂流程引擎 / 复杂拖拽式流程设计器
- SOP 智能编排引擎的完整实现
- 复杂智能推荐 / 完整流程自动生成
- 第三方真实资源绑定 / 批量项目创建

---

## 禁止事项

禁止：

- 把 Goal 直接写成 Workflow
- 把 SOP 直接等同于 Task
- 把 Workflow Template 和 Workflow Instance 混用
- 把 SOP 直接当作 WorkflowTemplate 或直接自动变成不可追踪的 Task 列表
- 把 Goal / SOP / Workflow 混为同一对象
- 将 Goal 设计为普通备注字段或自由文本
- 将「流程与任务」长期放在靠后步骤
- 在目标与交付模式未明确前优先配置执行 Agent
- 让流程配置失去其核心入口地位
- 为了通用性而把流程抽象得过空（优先让最小真实案例成立）
