---
alwaysApply: true
---

# 语言、协作与中文显示规则

## 默认语言

与用户协作时：

- 默认使用 **中文**
- 文档、解释、设计说明优先中文

允许保留英文的内容：

- 代码、变量名、文件名、API 字段、路由、接口名、技术关键词

但解释必须使用中文。

---

## 用户角色理解

用户角色是：

- 产品设计者
- 系统架构设计者
- 项目规划者

不是主要写代码的工程师。

输出优先顺序：

1. 产品结构
2. 系统架构
3. 模块边界
4. 页面设计
5. 开发步骤
6. 代码实现

不要直接跳到代码。

---

## 输出方式

复杂任务必须：

1. 先理解需求
2. 说明影响范围
3. 输出实施计划
4. 等待确认
5. 再进行代码修改

禁止：

- 直接大规模改代码
- 不解释设计就生成代码
- 修改不相关模块
- 推翻已有架构

---

## 前台中文显示要求

系统前台必须优先使用中文进行展示、说明和交互。前台界面不得直接暴露未经处理的英文技术标识。

### 1. 页面标题、按钮、标签、说明文案必须优先中文

### 2. 所有英文枚举值必须提供中文映射
包括但不限于：
- status、scopeType、planningMode、executionType
- intentType、roleType、category、executorStrategy
- sourceType、messageType

### 3. 业务对象展示名称必须使用中文
若对象内部使用英文 code 或 key，前台必须显示中文标签。

### 4. 表单帮助说明、空状态、错误提示必须使用中文

---

## 中文映射维护规则

中文映射应集中维护，建议通过以下方式管理：
- labels.ts
- displayMaps.ts
- optionLabels.ts
- i18n map 文件

禁止将大量中文映射散落在页面组件内部硬编码。

新增模块开发时：
- 新增英文枚举或状态，必须同步补充中文映射
- 新增前台页面，必须提供中文标题与中文说明
- 新增筛选项、按钮、状态标签，必须优先中文

---

## 禁止事项

禁止：
- 前台直接显示英文枚举值
- 前台只有英文按钮或英文状态
- 页面说明文案缺失中文版本
- 错误提示仅英文
