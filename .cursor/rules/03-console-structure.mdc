---
alwaysApply: true
---

# 控制台结构与租户隔离规则

## 平台后台（Platform Console）

### 职责
- 租户管理
- 平台用户管理
- 资源与配额
- 模板中心
- 审计日志
- 系统设置

### 菜单
- 平台工作台
- 租户管理
- 平台用户
- 资源与配额
- 模板中心
- 平台审计
- 平台设置

### 约束
- 不得出现租户业务页面（项目、任务、Agent 等属于租户后台）
- 不得依赖租户上下文，必须是全局视角
- 可查看/管理所有租户；租户列表、租户详情不依赖当前登录用户的 tenantId
- UI 风格与租户后台统一，但内容偏系统管理

---

## 租户后台（Tenant Console）

### 定位
企业 AI 工作中控台。

### 菜单
- 工作台
- 项目中心
- 任务执行
- 流程中心
- Agent中心
- Skills能力库
- 终端中心
- 数据分析
- 系统管理

菜单顺序体现业务流程：项目 → 流程 → Agent / Skill / Terminal → 任务执行 → 数据分析

### 约束
- 项目中心是业务入口，项目详情页是工作台
- 页面信息结构清晰，支持多模块扩展
- 便于后续接入 Agent / Skill / Workflow

---

## 租户数据隔离

### 核心原则
- **Project 归属租户**：`project.tenantId` 必填；列表、详情、创建、更新均在同一租户内
- 所有项目相关接口必须限定在**当前登录用户的 tenantId** 内
- tenantId 从 `useAuth().user?.tenant?.tenantId` 获取，不接收前端传入的任意 tenantId

### 实现要点
- 租户侧 projectService / projectRepository 请求时携带或隐含当前 tenantId（mock 层按 tenantId 过滤；真实 API 阶段由后端按 token 解析）
- 项目详情页路由 `/tenant/projects/:id`：进入前校验该项目属于当前租户；若不属于则 404 或重定向列表
- **ProjectMember**（`projectId`、`userId`、`roleInProject`、`joinedAt`）用于项目级权限与职责，当前阶段为预留
- 项目级角色（如 project_manager）的细粒度控制留到第二阶段；当前仅保证「租户内可见、跨租户不可见」

---

## 禁止事项

禁止：
- 租户后台请求中使用 URL 参数或全局状态覆盖 tenantId 以访问其他租户数据
- 平台后台使用「当前租户」做数据筛选
- 平台后台项目维度聚合与租户后台「当前租户项目」逻辑混用
- 跨控制台边界混放页面或功能
