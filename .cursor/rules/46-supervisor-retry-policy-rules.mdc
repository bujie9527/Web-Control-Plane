---
description: 执行监督重试策略规则，定义节点失败后的重试策略与限制
globs:
  - "**/*"
alwaysApply: true
---

# 执行监督重试策略规则

## 目标
在节点失败后，Supervisor 应根据节点配置的 retryPolicy 决定是否允许重试，以及如何重试。

## 核心原则

### 1. 节点是否允许重试，必须由模板策略决定
Supervisor 不得自由决定任何节点都可重试。

### 2. 重试必须有上限
必须支持：
- enabled
- maxRetryCount
- retryStrategy
- autoRetryEnabled

### 3. 重试次数必须记录到运行节点
WorkflowInstanceNode 必须记录：
- retryCount
- lastRecoveryAction
- recoveryStatus

### 4. 自动重试必须受限
本阶段仅允许有限自动重试。
不得无限循环重试。

## 前台要求
节点详情中必须显示：
- 是否允许重试
- 最大重试次数
- 当前重试次数
- 重试方式
- 是否允许自动重试

## 禁止事项
禁止：
- 无上限自动重试
- 未记录 retryCount 就重复执行
- 绕过策略直接重试
