---
description: 执行监督建议与运行态桥接规则，定义 Supervisor Decision 与 Runtime Center 的关系
globs:
  - "**/*"
alwaysApply: true
---

# 执行监督建议与运行态桥接规则

## 目标

建立 WorkflowRuntimeCenter 与 Execution Supervisor Agent 之间的桥接层，使运行态可以生成、展示、应用和忽略监督建议。

## 核心原则

### 1. Runtime Center 负责展示状态，Supervisor 负责生成建议

Runtime 不直接承担监督决策逻辑。
Supervisor 不直接替代 Runtime 页面操作。

### 2. 决策必须对象化

Supervisor 建议必须使用结构化对象表示，例如：

- WorkflowSupervisorDecision

禁止仅以自然语言消息代替正式决策对象。

### 3. 决策应用与忽略必须有状态

至少支持：

- suggested
- applied
- dismissed

### 4. 决策应用应回写 Runtime 状态与日志

当用户应用某个决策后，应至少：

- 更新实例或节点状态（mock 即可）
- 写入 WorkflowRuntimeLog

### 5. Phase 14 允许手动确认

本阶段监督建议默认应由用户确认后应用，不做完全自动执行。

## 页面要求

Runtime 详情页中必须可见：

- 当前监督建议
- 决策类型
- 中文原因说明
- 建议下一步
- 应用建议
- 忽略建议
- 重新分析

## 禁止事项

禁止：

- 将 Supervisor 建议仅隐藏在日志中
- 直接自动应用监督建议而不留状态记录
- 前台直接显示英文 decisionType 而无中文映射
