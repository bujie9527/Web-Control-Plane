---
alwaysApply: true
---

# 编码规范与 API 契约规则

## 修改范围原则

修改代码前必须：

1. 确认影响模块
2. 限制改动范围

不要修改不相关文件。

---

## 组件复用原则

新增页面前必须检查是否已有可复用的 shadcn/ui 组件。

优先复用：Button、Card、Table、Sheet（抽屉）、Dialog（弹窗）、Badge（状态标签）、Tabs、Form。

---

## 页面逻辑限制

页面组件不得包含：
- 复杂业务逻辑
- 第三方平台调用

必须通过 service / provider 处理。

---

## 前端分层约定

```
page（页面展示、调用 service）
  ↓
service（业务逻辑、数据组装）
  ↓
repository（数据读写，调用 /api/* 接口）
  ↓
schema（类型定义、状态枚举）
```

标准方法签名：

```typescript
// xxxService.ts
export const xxxService = {
  list(params: ListParams): Promise<PaginatedResult<Xxx>>,
  getById(id: string): Promise<Xxx>,
  create(data: CreateXxxInput): Promise<Xxx>,
  update(id: string, data: UpdateXxxInput): Promise<Xxx>,
  delete(id: string): Promise<void>,
  patchStatus(id: string, status: string): Promise<Xxx>,
}
```

---

## 后端路由拆分

后端路由按域拆分为独立文件：

```
server/
├── index.ts              — app 初始化 + 中间件 + 路由挂载
├── routes/
│   ├── authRoutes.ts
│   ├── projectRoutes.ts
│   ├── terminalRoutes.ts
│   ├── telegramRoutes.ts     ← 新增
│   ├── dataSourceRoutes.ts   ← 新增
│   ├── messageRoutes.ts      ← 新增
│   ├── schedulerRoutes.ts    ← 新增
│   ├── analyticsRoutes.ts    ← 新增
│   └── ...
```

每个路由文件导出 `registerXxxRoutes(app: Router)` 函数。

---

## 数据层约定

后端数据层使用 **Prisma + SQLite**（非 mock）。

- DB 操作文件：`server/domain/*Db.ts`
- 种子数据：`server/domain/*Seed.ts`，使用 `seedXxxIfEmpty()` 模式
- 动态 import：`await import('./domain/xxxDb')` 处理 Prisma 未初始化

前端直接调用 `/api/*` 接口，不再使用 mock service 作为主路径。

---

## Skill executionType 枚举

Skill 的执行方式为 4 种：

| 值 | 说明 |
|----|------|
| `llm` | 通过 llmExecutor 调用大模型 |
| `external_api` | 调用第三方服务（Tavily/Apify/Telegram 等） |
| `internal_api` | 调用系统自身 API |
| `hybrid` | LLM + API 的多步组合 |

> 旧值 `tool` 已迁移为 `external_api`。禁止使用 `tool`。

---

## 表单管理

所有表单必须使用 **react-hook-form + zod**：

```tsx
const schema = z.object({ name: z.string().min(1, '请输入名称') })
const form = useForm({ resolver: zodResolver(schema) })
```

禁止手动 useState 管理表单状态。

---

## 操作反馈

- 成功/失败/警告：使用 **sonner** toast
- 删除确认：使用 **Dialog** 二次确认弹窗
- 禁止使用 `alert()`、`window.confirm()`

---

## 复杂任务规则

复杂任务必须先输出计划，包括：
- 影响文件
- 新增模块
- 修改模块
- 风险点

确认后再执行。

---

## 完成后自检

修改完成后必须检查：
- 是否符合架构
- 是否符合 UI 规范（shadcn + Tailwind）
- 是否复用组件
- 是否破坏模块边界

---

## API 返回格式

所有 API 必须统一返回结构：

```json
{
  "code": 0,
  "message": "success",
  "data": {},
  "meta": {
    "requestId": "",
    "timestamp": ""
  }
}
```

---

## 资源接口风格

所有资源使用 REST 风格，标准接口：
- list / detail / create / update / delete / patchStatus

---

## ID 前缀约定

- Agent 模板 ID：`at-` 前缀
- Skill ID：`skill-` 前缀
- DataSourceProvider ID：`dsp-` 前缀
- SystemTerminalType ID：`stt-` 前缀

---

## 事件命名

统一事件命名：
- tenant.created / user.created / project.created
- workflow.created / task.created / task.started / task.failed
- agent.created / skill.updated
- terminal.connected / terminal.disconnected
- message.received / message.dispatched / message.replied

---

## 审计日志

所有关键操作必须预留 audit log，例如：
- 创建租户 / 删除项目 / 修改权限 / 更新 Agent
- 终端创建 / 凭证更新 / 定时任务配置
