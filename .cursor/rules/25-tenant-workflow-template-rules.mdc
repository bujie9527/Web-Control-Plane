---
description: 租户流程模板规则，定义平台模板复制与租户模板资产体系
globs:
  - "**/*"
alwaysApply: true
---

# Tenant Workflow Template 规则

## 目标
Tenant Workflow Template 是租户侧的流程模板资产。

该模块允许租户：

- 查看平台流程模板
- 复制平台模板
- 创建自己的流程模板
- 个性化调整节点配置
- 保存为租户流程资产

## 模板作用域

WorkflowTemplate 必须支持：

system
tenant

system 模板：
平台管理员创建，只读。

tenant 模板：
租户创建或复制生成，可编辑。

## 模板复制规则

租户复制 system 模板时：

scopeType = tenant
status = draft
version = 1
sourceTemplateId = systemTemplate.id
sourceVersion = systemTemplate.version

必须复制：

- WorkflowTemplateNode
- nodeCount
- 推荐 Agent
- Skill 约束

复制后租户模板成为独立对象。

## 可编辑字段

租户允许修改：

name
description
节点顺序
recommendedAgentTemplateId
allowedSkillIds
executorStrategy
reviewPolicy

## 禁止修改字段

租户不得修改：

supportedProjectTypeId
supportedGoalTypeIds
supportedDeliverableModes
sourceTemplateId
sourceVersion

## 模板状态

租户模板支持：

draft
active
archived

只有 active 模板可以用于项目创建。

## 来源信息

租户模板必须展示来源：

sourceTemplateId
sourceVersion

用于后续版本升级提示。
