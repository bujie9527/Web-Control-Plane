---
description: 中文显示与标签映射规则，要求前台优先中文展示
globs:
  - "**/*"
alwaysApply: true
---

# 中文显示与标签映射规则

## 目标
系统前台必须优先使用中文进行展示、说明和交互。

代码字段、类型名、路由、接口名可以使用英文，但前台界面不得直接暴露未经处理的英文技术标识。

## 必须遵守的要求

### 1. 前台页面标题、按钮、标签、说明文案必须优先中文
例如：
- 流程规划会话
- 流程草案
- 修改摘要
- 风险提示
- 确认生成模板

### 2. 所有英文枚举值必须提供中文映射
包括但不限于：
- status
- scopeType
- planningMode
- executionType
- intentType
- roleType
- category
- executorStrategy
- sourceType
- messageType

### 3. 业务对象展示名称必须使用中文
若对象内部仍使用英文 code 或英文 key，前台必须显示中文标签或中文说明。

### 4. 表单帮助说明、空状态、错误提示必须使用中文
不得仅显示英文原始信息。

### 5. 中文映射应集中维护
建议通过以下方式管理：
- labels.ts
- displayMaps.ts
- optionLabels.ts
- i18n map 文件

禁止将大量中文映射散落在页面组件内部硬编码。

## 特别要求
新增模块开发时：
- 若新增了英文枚举或英文状态，必须同步补充中文映射
- 若新增了前台页面，必须提供中文标题与中文说明
- 若新增了筛选项、按钮、状态标签，必须优先中文

## 禁止事项
禁止：
- 前台直接显示英文枚举值
- 前台只有英文按钮或英文状态
- 页面说明文案缺失中文版本
