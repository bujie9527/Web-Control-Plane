---
description: 租户作用域与跨租户隔离：项目归属租户、数据隔离、实现与禁止项
alwaysApply: true
---

# Tenant Scope Rules

## 关系与对象

- **Project 归属租户**：`project.tenantId` 必填；列表、详情、创建、更新均在同一租户内。
- **ProjectMember**（04 领域模型）：`projectId`、`userId`、`roleInProject`、`joinedAt`；用于项目级权限与职责，当前阶段为预留。

## 作用域约定

- **平台后台**：可查看/管理所有租户；租户列表、租户详情不依赖「当前登录用户的 tenantId」。
- **租户后台**：所有项目相关接口（list、detail、create、update、delete）必须限定在**当前登录用户的 tenantId** 内；从 `useAuth().user?.tenant?.tenantId` 取，不接收前端传入的任意 tenantId。

## 实现要点

- 租户侧 projectService / projectRepository 在请求列表或详情时，应携带或隐含当前 tenantId（mock 层按 tenantId 过滤；真实 API 阶段由后端按 token 解析 tenant）。
- 项目详情页路由 `/tenant/projects/:id`：进入前应校验该项目属于当前租户；若 id 不存在或不属于当前租户，应 404 或重定向列表，不展示跨租户数据。
- ProjectMember、项目级角色（如 project_manager）的细粒度控制留到第二阶段；当前仅保证「租户内可见、跨租户不可见」。

## 禁止

- 租户后台项目列表或详情请求中，使用 URL 参数或全局状态覆盖「当前用户的 tenantId」以访问其他租户项目。
- 平台后台项目维度的聚合（若未来有）应显式按 tenant 维度聚合，不与租户后台的「当前租户项目」逻辑混用。
