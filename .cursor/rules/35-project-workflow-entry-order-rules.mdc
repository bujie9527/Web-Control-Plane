---
description: 项目创建与编辑中的流程入口顺序规则，要求在目标确认后尽早确认流程
globs:
  - "**/*"
alwaysApply: true
---

# 项目流程入口顺序规则

## 目标
在项目创建与编辑流程中，流程与任务配置必须在目标确认后尽早出现，不能被放置到过晚位置。

## 核心原则

### 1. 目标确定后应立即确认流程
系统推荐顺序：
1. 项目基础信息
2. 目标与交付模式
3. 流程与任务
4. Agent 范围与协作偏好
5. 身份 / 渠道 / 资源
6. 预算与审核
7. 确认

### 2. 流程是目标的执行实现
流程模板、流程规划会话、任务拆分都应在目标与交付模式确定后进入配置。

### 3. Agent 范围配置应位于流程选择之后
因为 Agent 范围是对流程执行的支持性约束，而不是先于流程定义的核心对象。

## 页面要求
项目创建页与项目编辑页应保持大体一致的结构顺序，避免用户在创建与编辑时形成不同认知模型。

## 禁止事项
禁止：
- 将「流程与任务」长期放在靠后步骤
- 在目标与交付模式未明确前优先配置执行 Agent
- 让流程配置失去其核心入口地位
