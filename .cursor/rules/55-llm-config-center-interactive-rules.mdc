---
description: 模型配置中心交互规则，定义 LLM 配置中心必须支持的新增、编辑、启停与中文反馈能力
globs:
  - "**/*"
alwaysApply: true
---

# 模型配置中心交互规则

## 目标
`/system/llm-configs` 不应长期停留在只读或占位状态，必须升级为可交互的系统治理页面。

## 核心原则

### 1. Provider、ModelConfig、AgentBinding 必须支持真实交互
至少支持：
- 新增
- 编辑
- 启用 / 停用
- 状态反馈（提交中、成功、失败）

### 2. 所有交互必须中文化
表单、按钮、成功提示、错误提示、确认提示必须优先中文展示。

### 3. 页面结构可以延续现有三分区，但每个区块必须可操作
- 模型提供商
- 模型配置
- Agent 模型绑定

## 禁止事项
禁止：
- 长期保留 disabled 占位按钮而无实际交互
- 仅展示数据而无法修改
- 前台直接显示英文原始状态或错误

