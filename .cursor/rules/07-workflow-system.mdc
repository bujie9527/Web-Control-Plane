---
alwaysApply: true
---

# 流程系统规则（Workflow System）

## 1. Workflow / Task 执行模型

### 核心对象
- **WorkflowTemplate**：系统中的标准流程模板（流程定义层）
- **WorkflowTemplateNode**：模板节点定义
- **WorkflowInstance**：项目某次执行生成的实际流程实例（流程运行层）
- **WorkflowInstanceNode**：执行实例中的实际节点状态
- **Task**：业务层执行对象，必须关联 WorkflowInstance 或 WorkflowInstanceNode，不得作为孤立对象

### Identity 规则
Identity 属于 WorkflowInstance 上下文，不是 WorkflowTemplate 固有属性：
- 模板不绑定固定 Identity
- 实例运行时选择 Identity
- 节点与任务读取实例的 Identity 上下文

### 系统主链路
```
Project → WorkflowTemplate / WorkflowInstance → Node Binding → AgentTemplate → Skill → Executor → Terminal → Result
```

### 开发原则
- 先做线性流程，不做复杂分支
- 先做状态机，不做复杂自动调度
- 先做人工可控推进，不做完全自动执行

---

## 2. Workflow 模板工厂

### 模板结构维度

| 维度 | 字段 |
|---|---|
| 基础身份 | id, name, code, description |
| 作用域 | scopeType (system/tenant), tenantId |
| 生命周期 | status (draft/active/deprecated/archived), version, isLatest, isSystemPreset |
| 来源关系 | sourceTemplateId, sourceVersion, clonedFromTemplateId |
| 项目适配 | supportedProjectTypeId, supportedGoalTypeIds, supportedDeliverableModes, supportedChannels |
| 规划模式 | planningMode (manual/ai_assisted/hybrid) |
| 推荐执行 | recommendedAgentTemplateIds, recommendedSkillIds, defaultReviewPolicy |
| 统计 | nodeCount, createdAt, updatedAt |

### 核心原则
1. 模板必须强绑定项目语义边界（ProjectType + GoalTypes + DeliverableModes）
2. 流程可以细化目标，不能改写项目主目标语义
3. 必须支持结构化节点，不允许自由文本步骤
4. 支持 system / tenant 两层作用域
5. 流程定义层与运行层必须清晰分离

---

## 3. Workflow 节点构建

### 节点结构

每个 WorkflowTemplateNode 必须包含：

**执行类型（executionType）**：agent_task | human_review | approval_gate | result_writer | system_task（预留）| manual_input（预留）| branch_decision（预留）

**意图类型（intentType）**：create | review | search | research | publish | record | analyze | summarize | classify | reply

**顺序与依赖**：orderIndex, dependsOnNodeIds（前置节点完成后才可执行）

**能力绑定**：recommendedAgentTemplateId, allowedAgentRoleTypes, allowedSkillIds

**输入输出映射**：inputMapping, outputMapping（首版 JSON 表达式即可）

**执行策略**：executorStrategy, reviewPolicy, isOptional, onFailureStrategy, status

### 分层原则
- 节点定义"做什么类型的工作"
- Agent 定义"由谁来做"
- Skill 定义"具体用什么能力做"
- Executor 定义"如何执行"

四者必须分层建模，系统根据 intentType + allowedAgentRoleTypes 自动过滤可选 Agent 和 Skill。

---

## 4. 租户流程模板

### 模板作用域
- **system 模板**：平台管理员创建，只读
- **tenant 模板**：租户创建或复制生成，可编辑

### 复制规则
租户复制 system 模板时：scopeType=tenant, status=draft, version=1，必须复制节点、推荐 Agent、Skill 约束。复制后成为独立对象。

### 可编辑字段
name, description, 节点顺序, recommendedAgentTemplateId, allowedSkillIds, executorStrategy, reviewPolicy

### 禁止修改字段
supportedProjectTypeId, supportedGoalTypeIds, supportedDeliverableModes, sourceTemplateId, sourceVersion

### 模板状态
draft → active → archived。只有 active 模板可用于项目创建。必须展示来源信息（sourceTemplateId, sourceVersion）。

---

## 5. 流程规划会话

### 核心对象
1. **WorkflowPlanningSession**：会话容器
2. **WorkflowPlanningDraft**：草案版本
3. **WorkflowPlanningMessage**：结构化消息（用户/助手/系统）
4. **WorkflowPlanningDraftNode**：草案节点（兼容 WorkflowTemplateNode 结构）

### 核心原则
- 会话、草案、模板、实例四层严禁混用
- 每次修改必须形成草案版本，禁止直接覆盖
- 消息必须结构化存储，不是附属 UI 内容

### 工作台布局
- 左栏：规划上下文
- 中栏：消息会话
- 右栏：当前草案

---

## 6. 规划校验器（Planning Validator）

Validator 与 Planner 必须分离，面向结构化 Draft 工作。

### 校验范围

| 校验类型 | 内容 |
|---|---|
| 节点结构 | 节点存在性、key 唯一性、orderIndex 有效性、dependsOnNodeIds 引用有效性 |
| 节点类型 | executionType / intentType 在允许范围内 |
| Agent/Skill | recommendedAgentTemplateId 存在、allowedAgentRoleTypes 合法、allowedSkillIds 存在 |
| 项目边界 | 不越过 supportedProjectTypeId / supportedGoalTypeIds / supportedDeliverableModes |
| 可转化性 | DraftNode 具备转换为 TemplateNode 所需基础字段 |

### 输出要求
返回结构化结果：isValid, errors, warnings, normalizedSummary。前台中文展示（如"草案校验通过""存在无效节点类型""草案超出项目边界约束"）。

---

## 7. Draft → Template 转换

### 转换原则
DraftNode 必须转换为 WorkflowTemplateNode，禁止 Draft 与 Template 共用同一对象。

TemplateNode 必须具备：key, name, executionType, intentType, orderIndex, dependsOnNodeIds, recommendedAgentTemplateId, allowedSkillIds, executorStrategy

### 来源追踪
所有 Template 必须记录：sourcePlanningSessionId, sourcePlanningDraftId, sourceDraftVersion

---

## 8. 模板发布

### 发布流程
```
Draft → Validator → Confirm Publish → Create Template
```

### 发布权限
- 平台管理员：可发布 system 和 tenant 模板
- 租户用户：仅可发布 tenant 模板

### 发布记录（WorkflowTemplatePublishRecord）
planningSessionId, planningDraftId, draftVersion, templateId, publishedBy, publishedAt

---

## 统一禁止事项

- 创建不绑定 ProjectType / DeliverableMode 的流程模板
- 流程模板改变项目目标主语义
- 模板与运行实例混为一体
- 租户直接编辑平台模板
- 节点直接绑定自由 Prompt 代替 Agent/Skill
- 节点绕过约束选择任意 Agent / Skill
- 节点绕过模板边界访问终端
- 把流程规划页做成纯聊天页或把草案仅存前端临时状态
- 跳过 Validator 直接采用 Planner 结果或发布模板
- 直接在 UI 创建 Template 而绕过 Draft
- 发布后丢失来源信息
- 校验结果仅英文而无中文映射
