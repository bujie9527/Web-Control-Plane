---
description: Worker LLM 执行规则，定义 Worker 与 LLM 的调用边界
globs:
  - "**/*"
alwaysApply: true
---

# Worker LLM 执行规则

## 目标
Worker Agent 执行任务时必须通过统一通道调用 LLM，不得直接调用任何 LLM SDK。

## 核心约束

### 1. Worker 不得直接调用 LLM
禁止在 Worker 代码中：
- 直接 import OpenAI SDK
- 直接调用 openai.chat.completions.create
- 直接发起 fetch 到任意 LLM 提供商（除非经由 llmExecutor）

### 2. 必须通过 Adapter
Worker 执行链路：
```
WorkflowInstanceNode → Worker AgentTemplate → Worker Skill
  → Worker LLM Adapter → llmExecutor → Provider
```

### 3. 必须通过 llmExecutor
所有 LLM 调用必须经由：
- executeLLM(request: LLMExecuteRequest): Promise<LLMExecuteResult>

### 4. Provider 路由
llmExecutor 根据 modelConfig.provider 路由到对应 Provider：
- openai → callOpenAI
- openai_compatible → 兼容层（如 mock 或兼容 API）

## 禁止事项
禁止：
- Worker 或 Adapter 直接 import 或调用 OpenAI
- 绕过 llmExecutor 的 LLM 调用
- 在页面层发起 LLM 请求
