---
alwaysApply: true
---

# 开发进度追踪规则

## 目标
确保每次开发完成后，系统文档保持最新。

## 必须执行的更新

每次完成功能开发、Bug 修复、架构调整后，必须更新以下文档：

### 1. CHANGELOG（必须）
文件：`docs/status/CHANGELOG.md`

在文件顶部（在格式说明之后）追加新条目，格式：
```
## [YYYY-MM-DD] 变更标题
### 变更内容
- 具体变更项
### 影响范围
- 影响的模块/文件
### 方向调整（如有）
### 代码结构变化（如有）
```

### 2. 已完成功能清单（按需）
文件：`docs/status/completed-features.md`

当新增功能模块、完成 mock→真实数据迁移、或骨架页面真实化时，更新对应模块状态。

### 3. 产品蓝图（按需）
文件：`docs/status/product-blueprint.md`

当待开发项完成后，更新差距分析表中的对应行。

### 4. 代码索引（按需）
文件：`docs/status/codebase-index.md`

当新增目录、重命名模块、或新增重要文件时，更新目录结构说明。

### 5. 系统架构（按需）
文件：`docs/status/system-architecture.md`

当架构发生重大变化（新增 Shell、新增数据库模型、新增 API 资源域）时更新。

## 禁止事项

- 禁止跳过 CHANGELOG 更新
- 禁止在 docs/archive/ 中修改已归档文档
- 禁止创建新的 phase-N-*.md 格式的散落文档（统一使用 CHANGELOG）
