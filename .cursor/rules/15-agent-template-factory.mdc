---
description: Agent 模板工厂规则，定义平台级 AI 执行角色模板对象
globs:
  - "**/*"
alwaysApply: true
---

# Agent 模板工厂规则

## 目标
Agent Template Factory（Agent 模板工厂）是平台控制平面中的系统级模块，用于定义、治理、复制、派生、版本化、发布和下线可复用的 AI 执行角色模板。

它不是：
- 租户自有对象
- 聊天人格配置器
- 简单的 Prompt 模板仓库
- 运行时执行器
- 终端接入模块

它是：
- 平台级模板资产工厂
- AI 执行角色标准定义中心
- Workflow 与 Skill 之间的标准角色层
- 平台治理 AI 执行模板的控制模块

## 架构位置
系统必须维持以下主链路：

Project → Workflow → Agent Template / Agent Binding → Skill → Executor → Terminal → Result

其中：
- Agent Template 属于控制平面
- Agent Binding 属于项目/流程执行绑定关系
- Agent Template 不能越过 Skill / Executor 直接承担真实执行职责

## 核心原则
Agent Template 不是自由文本 Prompt 对象。
Agent Template 必须是结构化、可治理、可复用、可审计的平台级模板资产。

## Agent Template 必须具备的结构维度

### 1. 模板身份
- id
- name
- code
- description
- roleType

### 2. 模板分类
- category
- domain
- sceneTags

### 3. 工厂来源信息
- archetypeCode
- parentTemplateId
- sourceTemplateId
- sourceVersion

### 4. 生命周期信息
- status
- version
- isLatest
- isSystemPreset
- isCloneable

### 5. 适用范围
- supportedProjectTypeIds
- supportedGoalTypeIds

### 6. 能力授权
- supportedSkillIds
- defaultExecutorType
- allowedExecutorTypes
- allowedTerminalTypes

### 7. 模型配置
- defaultModelKey
- fallbackModelKeys
- temperature
- maxTokens

### 8. 提示与输出约束
- systemPromptTemplate
- instructionTemplate
- outputFormat

### 9. Guardrails 安全边界
- requireGoalContext
- requireIdentityContext
- requireSOPContext
- requireStructuredOutput
- disallowDirectTerminalAction

### 10. 审核策略
- requireHumanReview
- requireNodeReview
- autoApproveWhenConfidenceGte

### 11. 执行模式
- manual
- semi_auto
- full_auto

## 设计边界
禁止：
- 把 Agent Template 建模为 tenant-owned 对象
- 让 Agent Template 直接执行终端动作
- 把 Agent Template 简化成只包含 Prompt 的配置
- 让 Agent Template 与项目运行态强耦合
- 跳过结构化的授权与安全约束
- 让模板定义和租户运行时绑定混在同一对象里

## 必须遵守的设计要求
必须：
- 把 Agent Template 视为平台级模板资产
- 把它设计为可复制、可派生、可版本化、可发布的系统对象
- 支持未来模板工厂扩展
- 为 Workflow 节点绑定模板预留清晰接口
- 保持 Agent Template 与 Skill、Executor、Terminal 的职责分层
