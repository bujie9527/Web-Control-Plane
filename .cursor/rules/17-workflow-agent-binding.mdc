---
description: Workflow 节点与 Agent 模板绑定规则，定义租户消费方式与运行边界
globs:
  - "**/*"
alwaysApply: true
---

# Workflow 与 Agent 模板绑定规则

## 目标
Workflow 节点不能直接拥有一个可随意定义的 Agent。
Workflow 节点应通过 Agent Template Binding 的方式，绑定并消费平台已发布的 Agent 模板。

## 基本原则
1. 模板定义属于平台控制平面
2. 节点绑定属于项目执行平面
3. 租户只能绑定模板，不能定义模板
4. 节点只能在模板授权范围内进行有限覆盖
5. 节点绑定不能突破模板的系统级边界

## 建议绑定结构
WorkflowNode 或等效配置对象应支持：

- agentTemplateId
- selectedSkillIds
- executorStrategy
- reviewPolicyOverride
- templateSnapshot

## templateSnapshot 原则
当 WorkflowNode 绑定 AgentTemplate 后，应保留模板快照字段，用于防止未来模板升级直接影响历史项目运行一致性。

首版可以先使用 mock snapshot，但必须预留该能力。

## 租户允许覆盖的范围
租户在节点绑定时，可以在模板授权范围内有限配置：

- 选择模板允许范围内的部分 Skill
- 设置节点级 preferredExecutorType
- 设置节点级 reviewPolicyOverride（仅限允许范围）

## 租户禁止覆盖的范围
租户不能通过 WorkflowNode 绑定去修改或绕过以下模板边界：

- supportedProjectTypeIds
- supportedGoalTypeIds
- supportedSkillIds 的系统授权范围
- allowedExecutorTypes
- allowedTerminalTypes
- defaultModelKey 的平台权限边界
- Guardrails
- 模板核心系统 Prompt 规则

## 选择器要求
租户侧应通过 AgentTemplateSelector 或等效只读组件选择模板。

选择器必须：
- 只展示 active 模板
- 可根据 ProjectType / GoalType / NodeType 过滤
- 展示模板摘要信息
- 不提供模板编辑入口

## 设计边界
禁止：
- 在租户项目内创建平台级模板
- 在 Workflow 节点中临时写入完整 Agent 定义代替模板绑定
- 让节点绑定直接操作终端权限
- 让节点覆盖平台 Guardrails

## 结果要求
系统必须保证：
- WorkflowNode 绑定的是模板而不是自由 Agent
- 节点使用模板时具有一致性和可治理性
- 模板工厂与执行绑定关系明确分离
