---
description: Worker 与 Runtime 集成规则，定义执行、日志与失败处理
globs:
  - "**/*"
alwaysApply: true
---

# Worker 与 Runtime 集成规则

## 目标
Worker 执行必须与 Runtime Center 紧密集成，包括日志写入、状态更新与失败时的 Supervisor 接管。

## 核心原则

### 1. Worker 执行必须写 Runtime Log
执行开始、完成、失败必须写入 WorkflowRuntimeLog：
- worker_llm_execution_started
- worker_llm_execution_completed
- worker_llm_execution_failed

### 2. 失败必须进入 Supervisor
当：
- LLM 调用失败
- JSON 解析失败
- Schema 校验失败

触发：
- node.status = failed
- node.errorSummary = 错误摘要
- Supervisor analyze → recovery

### 3. Worker 只写允许字段
Worker 仅允许写入：
- WorkflowInstanceNode.resultSummary
- WorkflowInstanceNode.reviewSummary
- WorkflowInstanceNode.workerOutputJson（结构化结果供 UI 展示）
- 以及 status、completedAt、errorSummary 等运行时字段

不得：
- 直接写 WorkflowTemplate
- 直接写 WorkflowTemplateNode

### 4. 前台中文优先
执行状态、审核结果、失败原因、日志事件必须中文展示。

## 禁止事项
禁止：
- 无日志执行
- 失败时跳过 Supervisor
- Worker 越权写入 Template 层
