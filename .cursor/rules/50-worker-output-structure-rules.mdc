---
description: Worker 输出结构规则，定义 Worker 返回格式与校验要求
globs:
  - "**/*"
alwaysApply: true
---

# Worker 输出结构规则

## 目标
Worker Agent 的输出必须为结构化 JSON，且必须通过 Parser + Schema 校验后才能写入 Runtime。

## 核心原则

### 1. Worker 输出必须 JSON
所有 Worker LLM 调用必须要求模型返回 JSON 格式。

### 2. 必须 Schema 校验
- Content Creator：content、summary、tags、notes 必填
- Content Reviewer：reviewResult 必为 approved/revise，issues、suggestions、summary 存在

### 3. 解析失败不能写 Runtime
当：
- JSON 提取失败
- JSON 解析失败
- Schema 校验失败

不得写入 WorkflowInstanceNode.resultSummary 或 reviewSummary。
应进入 node.status = failed，由 Supervisor 处理。

### 4. 错误必须中文
Parser 校验错误必须提供 messageZh 供前台展示。

## 禁止事项
禁止：
- 未校验即写入 Runtime
- 解析失败时静默跳过
- 错误信息仅英文
