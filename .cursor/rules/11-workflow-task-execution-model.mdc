---
alwaysApply: true
---

# Workflow / Task Execution Model Rules

## 模块定位

Workflow / Task Execution 是本系统中“定义到执行”的桥梁层。

其职责是：

- 定义标准流程模板
- 生成具体执行实例
- 维护执行状态
- 连接项目、身份、任务与结果

---

## 当前阶段目标

当前阶段为 Workflow & Task Execution 1.0。

必须完成以下核心对象：

1. WorkflowTemplate
2. WorkflowTemplateNode
3. WorkflowInstance
4. WorkflowInstanceNode
5. Task 与 Workflow 的关系接入

---

## 非目标

当前阶段不做：

- 复杂拖拽式流程设计器
- 多分支复杂工作流引擎
- 全自动 Agent runtime 编排
- 第三方真实自动执行
- 深度审批系统

当前阶段重点是：

建立标准对象、状态流转和最小可运行闭环。

---

## 对象边界

### WorkflowTemplate
表示系统中的标准流程模板。

### WorkflowInstance
表示某个项目在某次任务创建中生成的实际流程实例。

### WorkflowTemplateNode
表示模板节点定义。

### WorkflowInstanceNode
表示执行实例中的实际节点状态。

### Task
Task 是业务层的执行对象，应与 WorkflowInstance 或 WorkflowInstanceNode 建立清晰关联，不得继续作为孤立对象存在。

---

## Identity 规则

Identity 属于 WorkflowInstance 上下文，而不是 WorkflowTemplate 固有属性。

即：

- 模板不绑定固定 Identity
- 实例运行时选择 Identity
- 节点与任务读取实例的 Identity 上下文

---

## 开发原则

- 先做线性流程，不做复杂分支
- 先做状态机，不做复杂自动调度
- 先做人工可控推进，不做完全自动执行
- 保持与 Project / Identity / Task / Result 的兼容关系
