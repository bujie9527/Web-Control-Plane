---
description: Agent 模型绑定编辑规则，定义如何为不同 Agent 设置主模型与备用模型
globs:
  - "**/*"
alwaysApply: true
---

# Agent 模型绑定编辑规则

## 目标
每个 Agent 使用哪条模型配置，必须可通过系统页面进行绑定编辑，不得继续依赖代码写死。

## 核心原则

### 1. Agent 绑定必须通过 AgentLLMBinding 管理
不得在 adapter 中继续保留长期写死的 `modelConfigId` 常量。

### 2. 每个 Agent 至少应支持一个主模型绑定
`bindingType`：
- `primary`
- `fallback`

### 3. 前台必须可配置主模型与备用模型
至少支持：
- 选择主模型
- 选择备用模型
- 启用 / 停用绑定

### 4. 绑定编辑应优先展示可用模型
仅展示已启用模型配置。
可根据 Agent 分类做过滤（planning / execution / coordination）。

### 5. 约束：同一 Agent 不得存在多个启用中的主绑定
当设置新主模型时，应替换旧主模型或禁用旧主绑定，保证唯一性。

## 禁止事项
禁止：
- 页面层直接修改 adapter 常量替代绑定关系
- 一个 Agent 同时存在多个激活中的主模型绑定
- 前台绑定类型无中文说明

