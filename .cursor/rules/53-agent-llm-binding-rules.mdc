---
description: Agent 与模型绑定规则，定义 AgentTemplate 如何绑定主模型与备用模型
globs:
  - "**/*"
alwaysApply: true
---

# Agent 与模型绑定规则

## 目标
AgentTemplate 使用哪条模型配置，必须通过正式绑定关系决定，而不是通过 adapter 内部常量写死。

## 核心原则

### 1. Agent 与模型绑定必须对象化
建议使用：
- AgentLLMBinding

至少包含：
- agentTemplateId
- modelConfigId
- bindingType
- priority
- isEnabled

### 2. 每个 Agent 至少应有一个主模型绑定
bindingType:
- primary
- fallback

### 3. Adapter 必须通过 binding service 获取模型
禁止在 adapter 中继续长期写死：
- llm-openai-main
- llm-openai-worker

### 4. 可支持不同 Agent 使用不同模型
例如：
- Planner → 主规划模型
- Creator → 生成模型
- Reviewer → 审核模型

## 禁止事项
禁止：
- 页面层直接决定某个 Agent 用哪个模型
- 在多个 adapter 中复制相同的 modelConfigId 常量
- 绕过 AgentBinding 直接访问配置
